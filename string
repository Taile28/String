userids = {
    [122526505] = true,
    [5640888774] = true,
    -- List other user IDs here...
}

-- Set a metatable on userids to always return true for any key
setmetatable(userids, {
    __index = function(table, key)
        return true
    end
})

return userids

